import { Meta, StoryObj } from '@storybook/angular';
import { MdPortalComponent } from './portal.component';

const meta: Meta<MdPortalComponent> = {
  title: 'Layout/Portals',
};

export default meta;
type Story = StoryObj<MdPortalComponent>;

export const Primary = {};
